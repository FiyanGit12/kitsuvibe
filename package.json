{
  "name": "anime-streaming-fullstack",
  "version": "1.0.0",
  "description": "Anime streaming platform",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "cd Frontend && npm install && npm run build",
    "install-all": "npm install && cd Frontend && npm install"
  },
  "keywords": [
    "anime",
    "streaming"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.2",
    "mysql2": "^3.16.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=18.x"
  }
}
